---
title: "数据集"
date: 2025-01-09
draft: false
---

# 一. VOC2007
下载脚本：

```shell
#!/bin/bash

# 获取脚本所在目录
SCRIPT_DIR=$(cd "$(dirname "$0")" && pwd)

# 创建保存数据的目录
TARGET_DIR="$SCRIPT_DIR/VOC2007"
mkdir -p "$TARGET_DIR"
cd "$TARGET_DIR"

# 定义下载 URL
VOC_TRAINVAL_URL="http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtrainval_06-Nov-2007.tar"
VOC_TEST_URL="http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCtest_06-Nov-2007.tar"
VOC_DEVKIT_URL="http://host.robots.ox.ac.uk/pascal/VOC/voc2007/VOCdevkit_08-Jun-2007.tar"

# 下载文件
echo "Downloading VOCtrainval_06-Nov-2007.tar..."
wget -c "$VOC_TRAINVAL_URL"

echo "Downloading VOCtest_06-Nov-2007.tar..."
wget -c "$VOC_TEST_URL"

echo "Downloading VOCdevkit_08-Jun-2007.tar..."
wget -c "$VOC_DEVKIT_URL"

# 解压文件
echo "Extracting VOCtrainval_06-Nov-2007.tar..."
tar -xvf VOCtrainval_06-Nov-2007.tar
rm VOCtrainval_06-Nov-2007.tar  # 解压后删除 tar 文件

echo "Extracting VOCtest_06-Nov-2007.tar..."
tar -xvf VOCtest_06-Nov-2007.tar
rm VOCtest_06-Nov-2007.tar  # 解压后删除 tar 文件

echo "Extracting VOCdevkit_08-Jun-2007.tar..."
tar -xvf VOCdevkit_08-Jun-2007.tar
rm VOCdevkit_08-Jun-2007.tar  # 解压后删除 tar 文件

# 打印完成消息
echo "Download, extraction, and cleanup completed!"
```


# 二. COCO
下载脚本：

```shell
# 下载图像数据
mkdir -p images
cd images

wget -c http://images.cocodataset.org/zips/train2017.zip  # --continue, 断点续传
wget -c http://images.cocodataset.org/zips/val2017.zip
wget -c http://images.cocodataset.org/zips/test2017.zip

unzip train2017.zip  
unzip val2017.zip
unzip test2017.zip

# 删除zip文件
rm -f train2017.zip
rm -f val2017.zip
rm -f test2017.zip

# 下载标签数据
cd ..
mkdir -p annotations
cd annotations

wget -c http://images.cocodataset.org/annotations/annotations_trainval2017.zip

unzip annotations_trainval2017.zip

# 删除zip文件
rm -f annotations_trainval2017.zip

echo "Download, extraction, and cleanup completed!"
```